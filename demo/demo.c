#include "../src/tea.h"
#include <apop.h>

void tea_c_tests();

int main(int argc, char **argv){
	tea_c_tests();
    /*
    apop_opts.thread_count = 2;
    gsl_rng *r = apop_rng_alloc(213);
 
    char *dbname,
         *configfile ="demo.spec";
    read_spec(&configfile, &dbname); //reads in the text file as well.
init_edit_list();

    apop_opts.verbose++;
    set_key_text("impute", "active_table", "viewdc");
    impute(NULL);

    char *tabname = "dc";
    do_preedits(&tabname);
*/
}
